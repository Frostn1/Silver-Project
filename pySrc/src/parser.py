import links
import structures
import functioncalls
import data

def parser(lexedTerms : list):
    lexemes = {'links':[],'structs':[],'data':[]}
    counter = 0
    while counter < len(src):

        if item[0] == '(':
            lexemes['links'].append(links.linkFile(item))
        elif item[0] == '<':
            while     
        counter += 1
            

    pass